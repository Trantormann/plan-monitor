{
    "name": "plan-monitor",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "preflight": "node scripts/check-env.mjs",
        "build": "tsc --project tsconfig.json",
        "bundle": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/plan-monitor.cjs",
        "watch": "tsc --project tsconfig.json --watch",
        "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true }); require('fs').rmSync('coverage', { recursive: true, force: true });\"",
        "test": "vitest run",
        "dev": "tsx src/index.ts",
        "start:daemon": "tsx src/index.ts start --background",
        "status": "tsx src/index.ts status",
        "debug": "tsx src/index.ts debug",
        "package:exe": "npm run build && npm run bundle && npx -y -p node@20.18.0 node scripts/package-sea.mjs dist/plan-monitor.exe",
        "package:bin": "npm run build && npm run bundle && npx -y -p node@20.18.0 node scripts/package-sea.mjs dist/plan-monitor.bin"
    },
    "devDependencies": {
        "@types/node": "^22.10.2",
        "esbuild": "^0.24.2",
        "postject": "^1.0.0-alpha.6",
        "tsx": "^4.15.4",
        "typescript": "^5.6.3",
        "vitest": "^2.1.8"
    }
}